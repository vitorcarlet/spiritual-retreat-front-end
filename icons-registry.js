import { addIcon } from "@iconify/react";
// Importa APENAS os dados JSON do ícone, não o componente
import homeFill from "@iconify/icons-eva/home-fill";

// 1. Registra o ícone com o nome que você quiser
addIcon("meu-home", homeFill);
