type RetreatsCardTableFilters = {
  period?: [string, string];
  name?: string;
  state?: string;
  city?: string;
  startDate?: string;
  endDate?: string;
  status?: string;
};

type RetreatsCardTableDateFilters = {
  period?: [string, string];
};
