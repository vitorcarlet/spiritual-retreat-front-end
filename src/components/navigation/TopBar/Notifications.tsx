import { Box } from "@mui/material";
import { Iconify } from "../../Iconify";

export const Notifications = () => {
  return (
    <Box>
      {/* Notification icon and logic can be implemented here */}
      <Iconify icon="mdi:bell" size={24} />
    </Box>
  );
};
